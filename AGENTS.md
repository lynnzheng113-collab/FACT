# Relativity P0 + P1 原型开发要求

本文件适用于本仓库全部文件。后续开发、修改、验证和提交必须遵守以下要求。

## 可维护性

1. 所有用户可见文案必须集中在 `src/constants/copy.ts`。
   - 包括标题、按钮、提示、状态、表头、表单标签、占位文案、无障碍标签、演示数据和可见数字。
   - 页面和组件只引用 `copy.ts`，不得在 JSX、HTML 或组件逻辑中散落可见文案。
2. 所有颜色和间距必须使用 CSS 变量，并集中定义在 `src/styles/vars.css`。
   - 新增颜色、间距或通用尺寸时，先在 `vars.css` 增加语义化变量，再在样式中引用。
   - 不在页面样式或组件内硬编码颜色和间距。

## 修改范围

3. 每轮只修改用户明确指出的内容。
4. 不顺手重构、改文案、换样式、升级依赖或整理无关文件。
5. 需要改动相邻代码才能完成需求时，只做最小必要改动，并在交付说明中明确原因。

## 提交规则

6. 用户明确说“提交”后，自动在本仓库执行：

   ```powershell
   git add -A
   git commit -m "本轮改动的简明描述"
   ```

7. 提交前核对暂存范围，避免提交 `node_modules`、临时文件或仓库外内容；提交后报告 commit hash 并确认工作区状态。
8. “提交”只表示创建本地 Git commit，不自动推送远端；只有用户明确要求时才 push。

## 每轮交付说明

9. 每次改完必须说明：
   - 修改了哪些文件；
   - 每个文件改了什么；
   - 如何撤回本轮改动。
10. 如果只是改词、改颜色、改数字等小改动，必须额外提供准确的文件路径和行号，方便用户自行修改。
11. 未经用户说“提交”，不要自动创建 commit。

## 规则沉淀

12. 用户以后新增、补充或修改项目开发要求时，在同一轮同步维护本文件。
13. 默认保留既有规则；只有用户明确要求替换、删除或新规则与旧规则冲突时才调整。发生冲突时，以用户最新的明确要求为准，并在交付说明中指出规则变化。
