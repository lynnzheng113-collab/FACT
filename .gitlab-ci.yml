deploy-pages:
  stage: deploy
  pages: true
  script:
    # dist is built and verified locally before it is pushed.
    - rm -rf public
    - mkdir -p public
    - cp -r dist/. public/
  artifacts:
    paths:
      - public/
